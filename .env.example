APP_ENV=development
APP_PROFILE=gtx1650
APP_UID=1000
APP_GID=1000

POSTGRES_DB=examguard
POSTGRES_USER=examguard
POSTGRES_PASSWORD=replace-with-a-strong-database-password
DATABASE_URL=postgresql+psycopg://examguard:replace-with-a-strong-database-password@postgres:5432/examguard

JWT_SECRET=replace-with-at-least-32-random-characters
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

UPLOAD_ROOT=/data/uploads
EVIDENCE_ROOT=/data/evidence
MODEL_ROOT=/models
CONFIG_ROOT=/app/configs
MAX_UPLOAD_BYTES=4294967296
FFPROBE_TIMEOUT_SECONDS=30

# Used only by the explicit `python -m app.cli.bootstrap_admin` command.
ADMIN_USERNAME=
ADMIN_PASSWORD=
ADMIN_FULL_NAME=

FRONTEND_PORT=8080
